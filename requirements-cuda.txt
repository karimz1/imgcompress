# GPU (CUDA) build variant of requirements.txt.
#
# Identical to requirements.txt EXCEPT onnxruntime is replaced by onnxruntime-gpu
# and the CUDA 12 / cuDNN 9 runtime libraries are pulled in as pip wheels so they
# live inside the portable venv (no CUDA base image required — the NVIDIA host
# provides the driver via `nvidia-container-toolkit` + `docker run --gpus all`).
#
# IMPORTANT: the onnxruntime-gpu <-> CUDA <-> cuDNN version triple is strict and
# must be verified on the target GPU. See:
# https://onnxruntime.ai/docs/execution-providers/CUDA-ExecutionProvider.html#requirements
werkzeug
cffi==2.0.0
pillow>=12.2.0
pycparser==3.0
pyheif==0.8.0
colorama==0.4.6
flask==3.1.3
apscheduler==3.11.2
granian>=2.7.7
pillow_heif==1.3.0
pypdfium2==5.10.1
psd-tools==1.17.4
rembg==2.0.75
onnxruntime-gpu==1.20.1
fpdf2==2.8.7

# CUDA 12 + cuDNN 9 runtime shared libraries (bundle the .so files onnxruntime-gpu
# dlopen()s at session creation: libcudart, libcudnn, libcublas, libcufft, libcurand).
nvidia-cuda-runtime-cu12
nvidia-cudnn-cu12
nvidia-cublas-cu12
nvidia-cufft-cu12
nvidia-curand-cu12
